from django.urls import path
from . import views

app_name = 'tables'

urlpatterns = [
    # Ajoutez ici les URLs de votre application tables
    # Exemple :
    # path('', views.index, name='index'),
]
